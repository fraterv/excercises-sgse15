var React = require('react');

var Header = React.createClass({
    render: function() {
        return (
            <h1>
                Enron Mail App
            </h1>
        );
    }
});

module.exports = Header;
